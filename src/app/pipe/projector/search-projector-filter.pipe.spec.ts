import {SearchProjectorFilterPipe} from './search-projector-filter.pipe';

describe('SearchProjectorFilterPipe', () => {
    it('create an instance', () => {
        const pipe = new SearchProjectorFilterPipe();
        expect(pipe).toBeTruthy();
    });
});
