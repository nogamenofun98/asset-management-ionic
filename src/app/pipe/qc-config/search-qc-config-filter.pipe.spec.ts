import {SearchQcConfigFilterPipe} from './search-qc-config-filter.pipe';

describe('SearchQcConfigFilterPipe', () => {
    it('create an instance', () => {
        const pipe = new SearchQcConfigFilterPipe();
        expect(pipe).toBeTruthy();
    });
});
