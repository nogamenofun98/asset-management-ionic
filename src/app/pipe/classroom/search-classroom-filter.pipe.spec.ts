import {SearchClassroomFilterPipe} from './search-classroom-filter.pipe';

describe('SearchClassroomFilterPipe', () => {
    it('create an instance', () => {
        const pipe = new SearchClassroomFilterPipe();
        expect(pipe).toBeTruthy();
    });
});
